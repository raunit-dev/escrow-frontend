import { AppHero } from '@/components/app-hero'

export default function DashboardFeature() {
  return (
    <div>
      <AppHero
        title="Hey, welcome to Escrow Demo Program"
        subtitle="Interact with Escrow smart contracts on Solana Devnet"
      />
    </div>
  )
}